/** @type {import('next').NextConfig} */
const nextConfig = {
    // i18n: {
    //     // locales: ['en', 'de'],
    //     defaultLocale: 'en',
    //     locales: ['en-US', 'fr', 'nl-NL', 'en'],
    // }
}

module.exports = nextConfig
